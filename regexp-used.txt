（1）查找正常断行  `([\.])\n` 替换： `$1\n\n`
（2）查找非正常断行  `([^\.])\n` 替换： `$1 `
（3）查找图解  `(Figure [\d+]:)`  替换： `\n\n$1` 
（4）查找参考文献条目的起始位置 `\n([A-Z][A-Za-z0-9\s,\.]*\(\d{4}\w?\))` 替换为 `\n\n$1`
（5）查找参考文献应用  `([A-Z][\w\s\.,、]*[,，]\s*\d{4}[abcd]*)`   替换为 ` $1 `